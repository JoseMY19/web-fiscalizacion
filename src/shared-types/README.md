Copia de `packages/shared-types/src` del monorepo, dentro del repo para que
el build de Vercel (que solo clona este repo) la encuentre. Si cambias algo
aquí, aplica el mismo cambio en la otra app y en `packages/shared-types`.
